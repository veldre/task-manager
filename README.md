# Task Manager API (Laravel 12)

A small Laravel 12 API project focusing on clean architecture (Actions + Repositories + DTOs) and automated testing.

## Requirements
- PHP 8.2+
- Composer

## Setup
```bash
composer install
cp .env.example .env
php artisan key:generate
php artisan migrate
